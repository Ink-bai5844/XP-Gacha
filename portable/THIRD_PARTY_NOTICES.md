# Third-party runtime notices

This portable distribution aggregates XP-Gacha with unmodified third-party runtimes and Python packages. They remain under their respective licenses.

- CPython is distributed under the Python Software Foundation License. The bundled runtime retains its original license files.
- MySQL Community Server is distributed by Oracle under GPLv2. The complete unmodified Windows ZIP runtime, including its `LICENSE` and `README`, is retained under `runtime/mysql`.
- Python package license metadata and package resources are retained in `runtime/python/Lib/site-packages`.
- Microsoft Visual C++ runtime DLLs are deployed application-locally to satisfy the native Windows runtime dependencies; their Microsoft license terms continue to apply.

See the bundled runtime directories and package `.dist-info` metadata for exact versions and license texts.
